Django==6.1.1
djangorestframework==3.18.0
djangorestframework-simplejwt==5.5.1
django-cors-headers==4.9.0
psycopg2-binary==2.9.12
Pillow==12.3.0
python-dotenv==1.2.3
argon2-cffi==23.1.0

# Production container serving
gunicorn==23.0.0
whitenoise==6.11.0

# Real-time (see Realtime.md) — ASGI/WebSocket transport for Messaging and
# Notifications. daphne replaces gunicorn as the container's entrypoint
# process; gunicorn is kept for any local/alternate deployment that only
# needs plain HTTP.
channels==4.3.2
channels-redis==4.3.0
daphne==4.2.3

# Google Sign-In (see google-login.md) — verifies GIS ID tokens server-side.
google-auth==2.35.0
requests==2.32.3
